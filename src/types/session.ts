export type SessionData = {
    sessionId: string;
/*

    patient: {
        topic: string;
        data: string;
        conversation?: Array<{
            timestamp: string;
            userInput: string;
            systemResponse: string;
        }>;
    };
*/
    lastUpdated: string;
};
